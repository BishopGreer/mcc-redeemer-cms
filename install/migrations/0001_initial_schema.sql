-- Migration 0001: Initial schema (baseline for 1.0.0)
-- This is automatically marked as applied during a fresh install.
-- For existing installs upgrading to the migrations system,
-- run this file only if your database predates the migrations table.

-- No schema changes — this entry just records that 1.0.0 is installed.
SELECT 1;
